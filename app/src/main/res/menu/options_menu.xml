<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/settingsFragment"
        android:menuCategory="secondary"
        android:title="@string/settings"
        app:showAsAction="never" />
    <!--menuCategory secondary will not go back to home destination
    when click up btn

   *** the id must match the fragment id which it will navigate to***-->

    <item
        android:id="@+id/termsAndConditions"
        android:title="@string/terms_amp_conditions"
        app:showAsAction="never" />
    <!-- here the id do not match the fragment id because we will
     navigate in a normal way using navController.navigate

     *** this is a global destination***-->
</menu>